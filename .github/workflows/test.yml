name: Test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        version:
          - 1.18
          - 1.19
          - 1.20

    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: ${{ version }}

    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
